# ==============================================
# OrderFlow - Environment Variables
# ==============================================

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3001

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=orderflow_db
DB_PORT=3306

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_REFRESH_SECRET=your_super_secret_refresh_key_change_this_in_production
JWT_EXPIRE=30m
JWT_REFRESH_EXPIRE=7d

# Google Maps API (FR09 - Distance Validation)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key
RESTAURANT_LATITUDE=7.0000000
RESTAURANT_LONGITUDE=80.0000000

# Email Configuration (SMTP) - FR15, FR27, FR28, FR31
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=noreply@orderflow.com

# Email Configuration (Resend) - recommended for production email verification
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM=onboarding@resend.dev
EMAIL_VERIFICATION_TTL_MINUTES=30
EMAIL_RESEND_COOLDOWN_SECONDS=60

# SMS Configuration (Optional - FR15, FR27, FR28)
SMS_PROVIDER=console
# Optional: default calling code used to normalize local numbers for SMS (E.164)
DEFAULT_COUNTRY_CALLING_CODE=+94
# For Twilio SMS (optional)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
# Recommended for production: API Key + Secret (safer rotation)
TWILIO_API_KEY=your_twilio_api_key
TWILIO_API_SECRET=your_twilio_api_secret
# Optional fallback for local testing only
TWILIO_AUTH_TOKEN=your_twilio_auth_token
# Recommended sender strategy at scale
TWILIO_MESSAGING_SERVICE_SID=MGXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# If not using Messaging Service, set a direct Twilio sender number
TWILIO_PHONE_NUMBER=+1234567890

# Payment Gateway - PayHere (FR30, FR31)
PAYHERE_MERCHANT_ID=your_payhere_merchant_id
PAYHERE_MERCHANT_SECRET=your_payhere_merchant_secret
PAYHERE_MODE=sandbox

# Payment Gateway - Stripe (FR30, FR31)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret
STRIPE_WEBHOOK_URL=http://localhost:3001/api/v1/payments/webhook/stripe

# System Settings
MAX_DELIVERY_DISTANCE_KM=15
SESSION_TIMEOUT_MINUTES=30
ORDER_AUTO_CANCEL_MINUTES=30
MIN_ORDER_AMOUNT=500

# Dynamic Delivery Fee Configuration (Distance-Based Pricing)
# Base delivery fee applies to all delivery orders
BASE_DELIVERY_FEE=100
# Distance within which only base fee applies (no distance surcharge)
FREE_DELIVERY_DISTANCE_KM=3
# Additional cost per kilometer beyond free delivery range
DELIVERY_FEE_PER_KM=20
# Maximum delivery fee cap to keep pricing reasonable
MAX_DELIVERY_FEE=300

# Tax Configuration (Set to 0 as per business decision)
TAX_PERCENTAGE=0

# File Upload
UPLOAD_DIR=./Assets
MAX_FILE_SIZE=5242880

# Cloudinary Image Uploads
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Logging
LOG_LEVEL=info

# Auth rate limiting
# Keep DISABLE_AUTH_RATE_LIMIT=false in production.
DISABLE_AUTH_RATE_LIMIT=false
# OTP request limiting
# In non-production, OTP limiter is disabled by default. Set to false to force-enable locally.
DISABLE_OTP_RATE_LIMIT=true
# Optional override for auth rate-limit max requests per 15 minutes.
# Defaults: production=10, non-production=100
AUTH_RATE_LIMIT_MAX=100
